# Full Spectrum Labs (Static Export)

This folder is a static HTML export generated from a WordPress database + uploads directory.

## Edit
- Edit pages: `*.html`
- Styles: `assets/css/styles.css`
- Media: `assets/uploads/`

## Deploy on GitHub Pages
1. Create a GitHub repo and commit everything in this folder.
2. In repo Settings → Pages, set Source to `main` and folder `/ (root)`.

